// Named import from @zevaui/tokens: extracted by scan-source.js (unit-tested
// directly), but NOT projected into components[] in v1 (design #156 D2).
import { spacingScale } from "@zevaui/tokens";

export const scale = spacingScale;

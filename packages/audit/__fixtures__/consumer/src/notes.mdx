# Notes

A syntactically valid DS import lives below, on purpose: `.mdx` is not in the
extension allowlist, so this must never contribute to the report.

import { Button } from "@zevaui/components";

<Button>Not scanned</Button>

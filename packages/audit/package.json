{
  "name": "@zevaui/audit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./report-schema": "./src/report-schema.js"
  },
  "scripts": {
    "build": "echo \"stage 1: build deferred (no dist)\"",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.tools.json",
    "test": "vitest run",
    "audit": "node scripts/audit-usage.js",
    "audit:gate": "node scripts/assert-usage-report.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@zevaui/config": "workspace:*"
  }
}
